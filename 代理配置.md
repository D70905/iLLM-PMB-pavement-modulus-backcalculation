🎯 问题确诊：代理配置问题
127.0.0.1:7897 是一个典型的本地代理端口，常见于：

科学上网工具（如ClashX、V2Ray等）
开发代理工具
企业网络代理

🚀 立即解决方案
方案A：重置代理设置（推荐）
matlab% 清除MATLAB的代理设置
java.lang.System.clearProperty('http.proxyHost');
java.lang.System.clearProperty('http.proxyPort');
java.lang.System.clearProperty('https.proxyHost');
java.lang.System.clearProperty('https.proxyPort');

% 或者显式设置为无代理
java.lang.System.setProperty('http.proxyHost', '');
java.lang.System.setProperty('http.proxyPort', '');
java.lang.System.setProperty('https.proxyHost', '');
java.lang.System.setProperty('https.proxyPort', '');

% 测试连接
config = loadBackcalculationConfig();
config.deepseek.disable_ssl_verify = true;
response = callLLMAPI('测试直连', config, 'deepseek');
方案B：检查系统代理设置
在命令提示符中运行：
cmdnetsh winhttp show proxy
如果显示有代理设置，重置它：
cmdnetsh winhttp reset proxy
方案C：启动您的代理软件
如果您确实需要使用代理（比如科学上网），请：

启动您的代理软件（Clash、V2Ray等）
确认代理运行在127.0.0.1:7897
然后重新测试

🔧 完整的重置脚本
让我为您创建一个完整的代理重置脚本：创建代理设置重置脚本resetProxyForLLM.m诊断代理连接故障，准备重置脚本和手动解决方案。用户按照我的建议执行了步骤1-3，然后在测试时出现了代理连接错误。错误显示系统试图连接到127.0.0.1:7897，这是一个本地代理端口，说明之前可能有代理软件在运行，现在没有运行了。
我已经创建了一个代理重置脚本来解决这个问题。现在我需要给用户一个清晰的操作指导：

首先运行代理重置脚本
如果仍有问题，提供手动解决方案
给出最直接的命令组合

这是一个典型的环境配置问题，不是代码问题，所以解决起来应该比较直接。太好了！现在找到根本问题了：您的系统配置了本地代理（127.0.0.1:7897），但代理服务没有运行。
这说明之前可能有科学上网工具或其他代理软件在运行，现在停止了，但系统仍然试图通过代理连接。
🎯 立即解决方案
选项1：运行代理重置脚本（推荐）
matlabresetProxyForLLM()
选项2：快速手动重置
matlab% 清除所有Java代理设置
java.lang.System.clearProperty('http.proxyHost');
java.lang.System.clearProperty('http.proxyPort');
java.lang.System.clearProperty('https.proxyHost');
java.lang.System.clearProperty('https.proxyPort');
java.lang.System.setProperty('java.net.useSystemProxies', 'false');

% 清除SSL验证
java.lang.System.setProperty('com.mathworks.net.ssl.disableHostnameVerification', 'true');

% 立即测试
config = loadBackcalculationConfig();
config.deepseek.disable_ssl_verify = true;
response = callLLMAPI('测试成功', config, 'deepseek')
选项3：如果您需要使用代理
如果您确实需要科学上网：

启动您的代理软件（Clash、V2Ray等）
确认它运行在127.0.0.1:7897端口
然后重新测试

🔧 系统级修复（在命令提示符中运行）
如果MATLAB方法不行，在管理员权限命令提示符中运行：
cmdnetsh winhttp reset proxy
ipconfig /flushdns
```

## 🎯 预期结果

修复成功后，您应该看到：
```
✓ LLM API调用成功，响应长度: XX 字符
📝 API响应: 测试成功
💡 为什么会出现这个问题
这通常发生在：

科学上网软件关闭但系统代理设置未重置
开发工具代理（如Fiddler、Charles）停止运行
企业代理配置变更

View your proxy reset tool
现在立即运行resetProxyForLLM()或使用选项2的快速命令，应该能立即解决问题！